for(var sumRef in filteredSummaries){
 								console.log(sumsInTag[sumRef]);
 								console.log(filteredSummaries[sumRef]);
 								if(sumsInTag[sumRef] != undefined){
 									console.log('equality');
 									//continue;
 								} else {
 									console.log('flagged');
 									var key = filteredSummaries[sumRef];
 									flaggedSumRefs[key] = key;
 								}
 							}
 							// check for differences between susInMeta and filteredSummaries
 							// remove every difference





 						for(var sumRef in allAvaliableSummaries){
 							console.log('===================');
 							console.log("sumRef:",sumRef);
 							var sumIndexRef = this.filter[tagMeta].sum_index_refs[sumRef].sum_index_ref;
 							console.log(cache.sumIndex[sumIndexRef]);
 							var sumMetaRef = cache.sumIndex[sumIndexRef].sum_meta_ref;
 							console.log(cache.sumMeta[sumMetaRef]);

 							// do summary contain all added filters?
 							// if not remove summary from filtered
 							console.log(cache.sumMeta[sumMetaRef].tag_index_refs);

							for (var key in filter) {
								if (filter[tagMetaKey].sum_index_refs.hasOwnProperty(key)){

								}
							}

 							for(var tagMeta in this.filter){
 							}

 							/*var flag = false;
 							for(var tagRef in cache.sumMeta[sumMetaRef].tag_index_refs){
 								var tagIndexRef = cache.sumMeta[sumMetaRef].tag_index_refs[tagRef].tag_index_ref;
 								if()
 							}*/

/*
 							for(var tagMeta in this.filter){
 								console.log("tagMeta",tagMeta);
 								var flag = false;
 								for(var sumIndexRef in this.filter[tagMeta].sum_index_refs){
 									console.log("avaliable == sumIndexRef",avaliable == sumIndexRef);

 									if(avaliable == sumIndexRef){
 										flag = true;
 									}
 								}
 								if(!flag){
	 								console.log("DELETED");
									delete filtered[avaliable];
								} else {
									console.log("SAVED");
								}
 							}
*/

 						}





 						for(var summary in filtered){
 							console.log(summary);
 						}


 						/*
 						for(var tagMeta in this.filter){
 							if(commonSummaries){
 								for(var sumIndexRef in this.filter[tagMeta].sum_index_refs){
 									if(commonSummaries[sumIndexRef]){
 										delete
 									}
 								}
 							} else {
 								for(var sumIndexRef in this.filter[tagMeta].sum_index_refs){
 									commonSummaries[sumIndexRef] = "12345";
									console.log("sumIndexRef:", sumIndexRef, commonSummaries[sumIndexRef]);
								}
 							}

 						}*/
 						/*for(var sum in this.sumData){
 							//console.log(sum);
 							if(this.filter[sum]){
 								console.log(this.filter[sum]);
 							} else {
 								console.log(this.sumData);
 							}
 						}*/